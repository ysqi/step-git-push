box: wercker/default
build:
  steps:
    - leipert/add-ssh-key-gh-bb@0.0.6:
       keyname: TEST_SSH
    - wercker/add-to-known_hosts@1.4.0:
       hostname: github.com
    - script:
       name: Run unit tests
       code: bash unittest.sh
    - script:
       name: Run integration tests
       code: bash integration.sh